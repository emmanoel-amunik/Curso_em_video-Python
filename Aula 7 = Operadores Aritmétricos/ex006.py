n = int(input('Type a number:'))
nd = n * 2
nt = n * 3
nr = n ** (1 / 2)
print('The double of {} is {}, the triple is {} and the square root is {:.2f}'.format(n, nd, nt, nr))

# we can also can do it without variables n this is a good idea to optimize the code
# but in the future... so, it depends on the situation
